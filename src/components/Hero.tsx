import React from 'react';

const Hero: React.FC = () => {
    return (
        <div className="hero">
            <h1>Welcome to KYRAI!</h1>
            <p>Your showcase starts here.</p>
            <button>Get Started</button>
        </div>
    );
};

export default Hero;